info:
  name: Jobs
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/sparkhire/jobs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

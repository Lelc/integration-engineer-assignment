info:
  name: Question sets
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/sparkhire/question-sets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

import { Button } from '@/components/ui/button';

export default function CreateOneWay() {
    return (
        <Button
            className={`bg-[#004ECB] font-roboto font-medium hover:bg-white hover:text-[#004ECB]`}
        >
            Create One-Way
        </Button>
    );
}
